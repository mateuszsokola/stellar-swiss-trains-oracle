// import { Spec } from "@stellar/stellar-sdk/contract";

// TODO(mateush): Mock. Remove it.
class Spec {}

export const swissRailwaysContractSpecs = new Spec([
  // TODO(mateush): Provide contract bindings in Base64
]);
